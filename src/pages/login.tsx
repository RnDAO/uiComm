import { emptyLayout } from "../layouts/emptyLayout";
import SEO from "../components/global/SEO";

function PageIndex(): JSX.Element {
  return (
    <>
      <SEO title="Login" />
      dsa
    </>
  );
}

PageIndex.pageLayout = emptyLayout;

export default PageIndex;
